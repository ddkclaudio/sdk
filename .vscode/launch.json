{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Main with Args",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "args": ["gen", "--dir", "schemas/", "--no-prompt"]
        }
    ]
}
